{
  "name": "@x-sls/example",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "serverless offline start --reloadHandler",
    "test": "jest",
    "lint": "eslint src --ext .js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@x-sls/env": "workspace:*"
  },
  "devDependencies": {
    "serverless": "^3.40.0",
    "serverless-offline": "^13.9.0"
  }
} 